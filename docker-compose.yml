services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-academy-frontend
    ports:
      - "80:80"
    restart: unless-stopped
    network_mode: "bridge"
    environment:
      - NODE_ENV=production
